{
  "license": "MIT",
  "scripts": {
    "sass": "sass --no-source-map ./src:./docs --watch",
    "stylelint": "stylelint \"**/*.scss\" --fix"
  },
  "devDependencies": {
    "stylelint": "13.6.1",
    "stylelint-config-standard": "20.0.0",
    "prettier": "2.0.5",
    "sass": "1.26.8"
  }
}
